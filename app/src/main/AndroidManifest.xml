<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.challenges">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyChallenges"></activity>
        <activity
            android:name=".ChallengeDetails3"
            android:parentActivityName=".ChallengesActivity"></activity>
        <activity
            android:name=".ChallengeDetails2"
            android:parentActivityName=".ChallengesActivity" />
        <activity
            android:name=".ChallengeDetails1"
            android:parentActivityName=".ChallengesActivity" />
        <activity android:name=".ChallengesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>